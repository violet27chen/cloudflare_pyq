{
  "name": "moments",
  "private": true,
  "version": "0.1.0",
  "description": "A single-author public moments feed. Only the author posts; everyone can read and like.",
  "type": "module",
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "dev": "pnpm --filter ./frontend dev",
    "dev:worker": "wrangler dev",
    "build:frontend": "pnpm --filter ./frontend build",
    "build": "pnpm build:frontend",
    "preview": "pnpm --filter ./frontend preview",
    "typecheck": "pnpm -r typecheck",
    "deploy": "pnpm build:frontend && node scripts/deploy.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_PASSWORD": {
        "description": "后台 /admin 的登录密码。建议填一个强密码；留空则由部署脚本自动随机生成（密码打印在构建日志中）。"
      },
      "ADMIN_JWT_SECRET": {
        "description": "JWT 会话令牌的签名密钥。建议留空由部署脚本自动生成（相当于 openssl rand -hex 32）；如自填需为随机长字符串。"
      }
    }
  }
}
